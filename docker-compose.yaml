version: '2'

services:
  web:
    build: web/
    volumes:
      - ./web:/code:ro
    restart:
      always

  celery:
    build: web/
    volumes:
      - ./web:/code:ro
    restart:
      always
    command:
      [celery, -A, app.tasks, worker]

  realtime_updater:
    build: web/realtime_updater/
    volumes:
      - ./web:/code:ro
    restart:
      always

  nginx:
    restart:
      always
    build:
      nginx/
    ports:
      - "80:80"
    volumes:
      - ./web/static:/www/static:ro
    depends_on:
      - web
